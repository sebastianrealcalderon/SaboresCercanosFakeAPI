{
  "name": "sabores-cercanos-fake-api",
  "version": "1.0.0",
  "description": "Fake API para Sabores Cercanos",
  "main": "db.json",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
